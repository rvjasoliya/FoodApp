// import 'dart:convert';

// import 'package:food/Api/RemoteService.dart';

// import '../Firebase/Indgredient.dart';
// import 'IngredientApi.dart';

// fetchAlbum(query, number, offset) async {
//   final response = await http.get(uri);

//    var post = Post.fromJson(jsonDecode(response.body));
//   if (listt.length == post.totalResults) {
//       print("Done");
//       for (var i = offset; i < post.totalResults; i++) {
//         (addIndgredient(
//           '${post.results[i].id}',
//           post.results[i].name,
//           post.results[i].image,
//           post.results[i].aisle,
//           '${post.results[i].possibleUnits}',
//         ));
//       }
// }
// import '../Firebase/Indgredient.dart';

// ingre(List po,var of,var ){
//    for (var i = offset; i < post.totalResults; i++) {
//    addIndgredient(
//           '${po.results[i].id}',
//           po.results[i].name,
//           po.results[i].image,
//           po.results[i].aisle,
//           '${po.results[i].possibleUnits}',
//         ));
//    }
// }